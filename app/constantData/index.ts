import { socialLinks, footerLinks, navLinks } from "./links";
import { heroData, cities, City, accordionContent } from "./constData";

export { socialLinks, footerLinks, heroData, navLinks, cities, City, accordionContent };
